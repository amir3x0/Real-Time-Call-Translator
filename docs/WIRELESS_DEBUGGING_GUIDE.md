# 📱 מדריך מקיף: Wireless Debugging עם Flutter

מדריך מקצה לקצה להגדרת פיתוח אלחוטי - ללא צורך בכבל USB!

---

## 📋 תוכן עניינים

1. [מבוא ורקע](#-מבוא-ורקע)
2. [דרישות מקדימות](#-דרישות-מקדימות)
3. [הגדרה ראשונית (חד פעמית)](#-הגדרה-ראשונית-חד-פעמית)
4. [שימוש יומיומי](#-שימוש-יומיומי)
5. [Hot Reload - הקסם של Flutter](#-hot-reload---הקסם-של-flutter)
6. [פתרון בעיות](#-פתרון-בעיות)
7. [טיפים מתקדמים](#-טיפים-מתקדמים)

---

## 🎯 מבוא ורקע

### מה זה Wireless Debugging?

במקום לחבר את הטלפון למחשב בכבל USB בכל פעם שרוצים לבדוק שינויים, אנחנו יוצרים חיבור **אלחוטי דרך Wi-Fi**.

### למה זה שימושי?

| בעיה עם USB | הפתרון האלחוטי |
|------------|----------------|
| 🔌 כבל מפריע על השולחן | חופש תנועה מלא |
| 📱 לא יכול לבדוק את הטלפון בזמן שמחובר | הטלפון ביד כמו משתמש אמיתי |
| 🔄 צריך לנתק/לחבר כל פעם | מחובר כל היום אוטומטית |
| 💻 רק פורט USB אחד | כמה מכשירים במקביל |

### איך זה עובד?

```
רגיל (USB):
┌──────────┐  USB Cable  ┌──────────┐
│ Computer │◄───────────►│  Phone   │
└──────────┘             └──────────┘

אלחוטי (Wi-Fi):
┌──────────┐             ┌──────────┐
│ Computer │             │  Phone   │
│192.168.1.│◄──── WiFi ──►│192.168.1.│
│   100    │  Port 5555  │    42    │
└──────────┘             └──────────┘
      │                        │
      └────── Router ──────────┘
```

**התנאי העיקרי:** המחשב והטלפון חייבים להיות **על אותה רשת Wi-Fi**.

---

## ✅ דרישות מקדימות

### במחשב:

- [x] **Flutter SDK** מותקן ועובד
- [x] **ADB** מותקן (מגיע עם Android SDK)
- [x] חיבור לרשת **Wi-Fi**

### בטלפון:

- [x] **Android 5.0+** (API 21+)
- [x] **מצב מפתח (Developer Mode)** מופעל
- [x] **USB Debugging** מופעל
- [x] חיבור לאותה רשת **Wi-Fi** כמו המחשב
- [x] כבל USB (רק להגדרה הראשונית!)

### איך מפעילים Developer Mode?

1. פתח **הגדרות** > **אודות הטלפון**
2. לחץ **7 פעמים** על **מספר Build**
3. תראה הודעה: "You are now a developer!"
4. חזור להגדרות > תראה **אפשרויות מפתח**
5. הפעל **USB Debugging**

---

## 🔧 הגדרה ראשונית (חד פעמית)

> ⚠️ **שים לב:** שלב זה דורש חיבור USB פעם אחת. אחרי זה לא תצטרך יותר!

### שלב 1: חבר את הטלפון ב-USB

חבר את הטלפון למחשב עם כבל USB.

אם זו הפעם הראשונה, הטלפון ישאל אם לאשר חיבור - **אשר ותסמן "Always allow"**.

### שלב 2: ודא שהמחשב מזהה את הטלפון

פתח PowerShell או CMD והרץ:

```cmd
adb devices
```

**צפי לפלט:**
```
List of devices attached
RF8M53XKHVH     device
```

> 🔴 **אם רואים `unauthorized`:** בטלפון, אשר את חלון ה-debugging.
> 
> 🔴 **אם הרשימה ריקה:** ודא ש-USB Debugging מופעל בטלפון.

### שלב 3: הפעל מצב TCP/IP

הפקודה הזו אומרת לטלפון להתחיל להאזין לחיבורים דרך הרשת:

```cmd
adb tcpip 5555
```

**צפי לפלט:**
```
restarting in TCP mode port: 5555
```

**מה קורה כאן?**
- `adb` - הכלי לתקשורת עם Android
- `tcpip` - מצב תקשורת דרך רשת (במקום USB)
- `5555` - מספר הפורט שהטלפון יאזין עליו (ברירת מחדל)

### שלב 4: מצא את כתובת ה-IP של הטלפון

**אפשרות א' - דרך הטלפון:**
1. הגדרות > Wi-Fi
2. לחץ על הרשת שאתה מחובר אליה
3. חפש **IP Address** (למשל: `192.168.1.42`)

**אפשרות ב' - דרך ADB:**
```cmd
adb shell ip addr show wlan0 | findstr "inet "
```

**צפי לפלט:**
```
    inet 192.168.1.42/24 brd 192.168.1.255 scope global wlan0
```

> 📝 **שמור את ה-IP!** תצטרך אותו (למשל: `192.168.1.42`)

### שלב 5: נתק את ה-USB והתחבר אלחוטית!

**נתק את כבל ה-USB** מהטלפון.

עכשיו התחבר אלחוטית:

```cmd
adb connect 192.168.1.42:5555
```

> ⚠️ **החלף `192.168.1.42` ב-IP של הטלפון שלך!**

**צפי לפלט:**
```
connected to 192.168.1.42:5555
```

### שלב 6: ודא שהחיבור עובד

```cmd
adb devices
```

**צפי לפלט:**
```
List of devices attached
192.168.1.42:5555       device
```

🎉 **מזל טוב! הטלפון מחובר אלחוטית!**

---

## 🚀 שימוש יומיומי

כל בוקר (או אחרי restart של הטלפון), הרץ:

### שלב 1: התחבר לטלפון

```cmd
adb connect 192.168.1.42:5555
```

### שלב 2: ודא חיבור

```cmd
adb devices
```

### שלב 3: הרץ את האפליקציה

```cmd
cd d:\studies\Final Project\Real-Time-Call-Translator\mobile
flutter run
```

Flutter יזהה אוטומטית את הטלפון וישאל עליו בחר.

### שלב 4: פתח ושנה קוד!

- פתח קובץ `.dart` ב-VS Code
- עשה שינוי
- שמור (Ctrl+S)
- בטרמינל לחץ **`r`** ל-Hot Reload

**זהו! השינוי יופיע מיידית בטלפון! 🔥**

---

## 🔥 Hot Reload - הקסם של Flutter

### מה זה Hot Reload?

במקום לבנות את כל האפליקציה מחדש בכל שינוי (שלוקח דקות), Flutter שולח **רק את מה שהשתנה** לטלפון.

### הפקודות בזמן ש-`flutter run` פעיל:

| מקש | פעולה | זמן | State |
|-----|-------|-----|-------|
| **`r`** | Hot Reload | ~500ms | ✅ נשמר |
| **`R`** | Hot Restart | ~3-5s | ❌ מתאפס |
| **`q`** | יציאה | - | - |
| **`d`** | Detach (נשאר רץ) | - | - |

### מתי להשתמש ב-`r` (Hot Reload)?

- שינויי **UI** (צבעים, טקסטים, גדלים)
- שינויים בפונקציית **`build()`**
- שינויים ב-**Widgets**
- שינויים ב-**Styles**

**דוגמה:**
```dart
// שינוי מ:
Text('Hello')
// ל:
Text('Hello World!')
// → לחץ 'r' → מיידי!
```

### מתי להשתמש ב-`R` (Hot Restart)?

- שינויים ב-**`initState()`**
- שינויים ב-**`main()`**
- הוספת **משתנים חדשים** לקלאס
- שינויים ב-**`const`** values
- שינויים ב-**enums**

**דוגמה:**
```dart
// שינוי מ:
static const int maxItems = 10;
// ל:
static const int maxItems = 20;
// → צריך 'R' כי זה const
```

### Hot Reload לא יעבוד על:

- ❌ קוד **Native** (Java/Kotlin/Swift)
- ❌ קבצי **pubspec.yaml** (צריך restart מלא)
- ❌ קבצי **assets** חדשים
- ❌ **Platform channels**

---

## 🔧 פתרון בעיות

### בעיה 1: "Connection refused"

```
error: cannot connect to 192.168.1.42:5555: Connection refused
```

**גורם:** הטלפון לא במצב TCP/IP.

**פתרון:** חבר ב-USB והפעל מחדש:
```cmd
adb tcpip 5555
adb connect 192.168.1.42:5555
```

---

### בעיה 2: "No devices found"

```cmd
adb devices
# List of devices attached
# (ריק)
```

**גורמים אפשריים:**
1. הטלפון לא באותה רשת Wi-Fi
2. Firewall חוסם
3. הטלפון עבר ל-sleep ואיבד את מצב TCP/IP

**פתרון:**
```cmd
# חבר ב-USB
adb tcpip 5555
# נתק USB
adb connect 192.168.1.42:5555
```

---

### בעיה 3: "Device offline"

```
List of devices attached
192.168.1.42:5555       offline
```

**פתרון:**
```cmd
adb disconnect 192.168.1.42:5555
adb connect 192.168.1.42:5555
```

---

### בעיה 4: ה-IP של הטלפון השתנה

רשתות Wi-Fi יכולות לתת IP שונה כל פעם.

**פתרון:** בדוק את ה-IP הנוכחי ב:
- הגדרות > Wi-Fi > פרטי הרשת

או הגדר **Static IP** לטלפון בהגדרות ה-Wi-Fi.

---

### בעיה 5: Hot Reload לא מגיב

**פתרון א':** נסה Hot Restart במקום:
```
בטרמינל לחץ: R (גדולה)
```

**פתרון ב':** הפעל מחדש את flutter run:
```
לחץ: q
flutter run
```

---

## 💡 טיפים מתקדמים

### 1. קיצור להתחברות מהירה

צור קובץ `connect.bat` בתיקיית הפרויקט:

```batch
@echo off
echo Connecting to phone...
adb connect 192.168.1.42:5555
adb devices
pause
```

כל בוקר פשוט הרץ את הקובץ.

---

### 2. כמה מכשירים במקביל

אם יש לך כמה טלפונים:

```cmd
adb connect 192.168.1.42:5555  # טלפון 1
adb connect 192.168.1.43:5556  # טלפון 2 (פורט אחר)

# הרץ על מכשיר ספציפי:
flutter run -d 192.168.1.42:5555
```

---

### 3. לוגים בזמן אמת

בטרמינל נפרד:
```cmd
adb logcat | findstr "flutter"
```

או:
```cmd
flutter logs
```

---

### 4. שמירה אוטומטית → Hot Reload אוטומטי

ב-VS Code הגדר:
- File > Preferences > Settings
- חפש "Auto Save"
- הגדר ל-"afterDelay" עם 1000ms

עכשיו כל שינוי יישמר אוטומטית ו-Hot Reload יקרה לבד!

---

### 5. Profile/Release Mode אלחוטית

```cmd
# Profile mode (לבדיקת ביצועים)
flutter run --profile

# Release mode (כמו production)
flutter run --release
```

---

## 📝 סיכום מהיר

### Setup חד-פעמי (עם USB):
```cmd
adb devices          # ודא שמזוהה
adb tcpip 5555       # הפעל מצב רשת
# נתק USB
adb connect IP:5555  # התחבר אלחוטית
```

### כל יום:
```cmd
adb connect 192.168.1.42:5555
cd mobile
flutter run
# עכשיו שנה קוד ולחץ 'r' לעדכון מיידי!
```

---

## 🔗 קישורים שימושיים

- [Flutter Hot Reload Documentation](https://docs.flutter.dev/tools/hot-reload)
- [Android ADB Documentation](https://developer.android.com/tools/adb)
- [Flutter Debugging Guide](https://docs.flutter.dev/testing/debugging)

---

*מדריך זה נכתב עבור פרויקט Real-Time Call Translator*
*עודכן לאחרונה: ינואר 2026*
